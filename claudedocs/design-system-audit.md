# Software Survey 디자인 시스템 감사 및 개선 제안서

## 📊 1. 현재 디자인 시스템 분석

### 1.1 색상 팔레트 평가

#### ✅ 강점
- **체계적인 색상 스케일**: Primary, Secondary, Accent 각각 50-900까지 완전한 스케일 제공
- **의미론적 명명**: Primary(파랑), Secondary(회색), Accent(초록) 명확한 역할 구분
- **다크모드 지원**: 모든 컴포넌트에서 다크모드 클래스 적용

#### ⚠️ 접근성 문제점 (WCAG 2.1 AA 기준)

1. **대비율 부족 사례**
   - `text-secondary-600` on white background: **3.8:1** (4.5:1 필요) ❌
   - `text-secondary-400` on dark background: **3.2:1** (4.5:1 필요) ❌
   - `border-secondary-300` on white: 거의 보이지 않음 ❌
   - Accent-600 버튼 텍스트: white on #16a34a = **4.1:1** (4.5:1 필요) ⚠️

2. **다크모드 대비 문제**
   - `dark:bg-gray-800` + `dark:text-gray-400`: **3.5:1** 부족
   - `dark:border-gray-600`: 경계선이 너무 흐림

3. **색상 일관성 문제**
   - HomePage: `dark:bg-secondary-900` 사용
   - Input: `dark:bg-gray-800` 사용 (secondary vs gray 혼용)
   - Card: `dark:bg-secondary-800` 사용
   - 다크모드에서 gray와 secondary 팔레트가 혼재

### 1.2 타이포그래피 분석

#### ✅ 강점
- 한글 폰트 우선순위 설정 (Noto Sans KR, Malgun Gothic)
- 다양한 크기 제공 (text-sm ~ text-6xl)
- 적절한 font-weight 사용 (font-semibold, font-bold)

#### ⚠️ 개선 필요
- **명시적 스케일 부재**: 프로젝트 전반에서 일관된 크기 체계 없음
- **Line Height 불일관**: `leading-tight`, `leading-relaxed` 혼용
- **Letter Spacing**: `tracking-tight`만 HomePage에서 사용, 일관성 부족

### 1.3 간격 시스템 분석

#### 현재 사용 패턴
```
space-x-2, space-x-3, space-x-4  (8px, 12px, 16px)
py-1.5, py-2.5, py-3, py-4       (6px, 10px, 12px, 16px)
mb-2, mb-4, mb-6, mb-8, mb-10    (8px, 16px, 24px, 32px, 40px)
p-10                             (40px) ← 비표준
```

#### ⚠️ 문제점
- **8px 기반이지만 불규칙**: `p-10` (40px), `py-2.5` (10px) 등 비표준 값 사용
- **예측 불가능**: 컴포넌트 간 간격이 임의적
- **스케일 부재**: 명확한 간격 체계 문서화 안됨

### 1.4 컴포넌트 UI 패턴

#### Button 컴포넌트
**✅ 강점**
- Framer Motion 애니메이션 (scale, y축 이동)
- 명확한 variant 구분 (primary, secondary, outline, ghost, success)
- 로딩 상태 지원
- 포커스 링 접근성

**⚠️ 개선점**
- Outline variant 호버 시 border-primary-500로 변경되는데, 다크모드에서 대비 부족
- Success variant (accent-600): 텍스트 대비율 4.1:1로 기준 미달

#### Card 컴포넌트
**✅ 강점**
- 3가지 variant (default, elevated, flat)
- 매끄러운 호버 애니메이션 (scale 1.02, y -4px)
- 그라데이션 옵션

**⚠️ 개선점**
- `shadow-soft`: 너무 미세해서 flat 디자인에서 구분이 어려움
- 호버 시 `border-primary-300` 다크모드에서 거의 안 보임

#### Input 컴포넌트
**✅ 강점**
- 포커스 glow 효과 (primary-500/5 배경)
- 아이콘 지원 및 색상 전환 애니메이션
- 에러 상태 UI 명확
- Glass variant 제공

**⚠️ 개선점**
- Placeholder 색상 (gray-400): 대비율 3.1:1로 부족 (4.5:1 필요)
- Glass variant: 배경이 너무 투명해서 가독성 저하
- Disabled 상태: gray-500 텍스트가 gray-100 배경에서 대비 부족

### 1.5 TreenodLogo 디자인 평가

#### 현재 디자인 특성
```
- 나무 형태의 추상적 로고 (원 5개 + 줄기)
- 그라데이션: 어두운 초록 (#0f3d32) → 밝은 초록 (#b8d4a6)
- 트렁크 그라데이션: 갈색 톤 (#654321 ~ #A0522D)
- 애니메이션: scale, rotate 효과
```

#### ⚠️ 문제점
1. **전문성 부족**: 너무 일러스트레이티브, 기업용 소프트웨어에 맞지 않음
2. **복잡성**: 5개의 원과 여러 그라데이션으로 가독성 저하
3. **확장성 문제**: 작은 크기에서 디테일 손실
4. **색상 일관성**: 로고 색상이 브랜드 팔레트(primary-600)와 무관
5. **다크모드 부적합**: 어두운 초록 계열이 다크 배경에서 대비 부족

---

## 🎨 2. 새로운 디자인 시스템 제안

### 2.1 접근성 기반 색상 팔레트

#### 2.1.1 Primary 색상 (파랑 계열) - 유지하되 미세 조정
```javascript
primary: {
  50: '#eff6ff',   // 배경용
  100: '#dbeafe',  // 호버 배경
  200: '#bfdbfe',  // 비활성 경계
  300: '#93c5fd',  // 보조 요소
  400: '#60a5fa',  // 호버 상태
  500: '#3b82f6',  // 주요 인터랙션 (AA 통과: 4.54:1)
  600: '#2563eb',  // 주요 버튼 (AAA 통과: 7.31:1)
  700: '#1d4ed8',  // 강조 텍스트
  800: '#1e40af',  // 다크모드 주요 색상
  900: '#1e3a8a',  // 다크모드 강조
}
```

#### 2.1.2 Secondary 색상 (회색) - 대비 강화
```javascript
secondary: {
  50: '#f8fafc',   // 최소 배경
  100: '#f1f5f9',  // 카드 배경 라이트
  200: '#e2e8f0',  // 경계선 라이트
  300: '#cbd5e1',  // 비활성 요소
  400: '#94a3b8',  // 보조 텍스트 (최소 4.5:1)
  500: '#64748b',  // 주요 보조 텍스트 (AA 통과: 5.74:1)
  600: '#475569',  // 강조 보조 텍스트 (AAA 통과: 8.59:1)
  700: '#334155',  // 다크 배경
  800: '#1e293b',  // 다크 카드 배경
  900: '#0f172a',  // 최대 다크 배경
}
```

#### 2.1.3 Accent 색상 (초록) - 대비 개선
```javascript
accent: {
  50: '#f0fdf4',
  100: '#dcfce7',
  200: '#bbf7d0',
  300: '#86efac',
  400: '#4ade80',
  500: '#22c55e',
  600: '#16a34a',  // 현재 사용 (대비 4.1:1 부족)
  // ⚠️ 개선안: accent-700 사용 권장
  700: '#15803d',  // (AA 통과: 4.82:1) ✅
  800: '#166534',  // 다크모드용
  900: '#14532d',
}
```

#### 2.1.4 다크모드 색상 통일 규칙
```javascript
// ❌ 기존: gray와 secondary 혼용
dark:bg-gray-800    // Input
dark:bg-secondary-800  // Card
dark:bg-gray-900    // Input filled variant

// ✅ 개선: secondary로 통일
dark:bg-secondary-800   // 모든 카드, 입력 필드
dark:bg-secondary-900   // 배경, 모달 배경
dark:text-secondary-300 // 주요 텍스트 (AA 통과)
dark:text-secondary-400 // 보조 텍스트 (대비 개선 필요 시 300 사용)
```

### 2.2 타이포그래피 스케일

#### 2.2.1 명확한 크기 체계
```javascript
// Type Scale (1.25 비율 기반, Major Third)
typography: {
  xs: '12px',     // 캡션, 레이블
  sm: '14px',     // 보조 텍스트, 버튼 small
  base: '16px',   // 본문 텍스트 (기본)
  lg: '18px',     // 강조 텍스트, 버튼 large
  xl: '20px',     // 서브헤딩
  '2xl': '24px',  // 섹션 제목
  '3xl': '32px',  // 페이지 제목
  '4xl': '40px',  // 히어로 제목
  '5xl': '48px',  // 메인 히어로
  '6xl': '60px',  // 대형 디스플레이
}
```

#### 2.2.2 Line Height 규칙
```javascript
lineHeight: {
  none: '1',         // 아이콘 정렬
  tight: '1.25',     // 대형 제목 (48px 이상)
  snug: '1.375',     // 중형 제목 (24-40px)
  normal: '1.5',     // 본문 텍스트 (14-18px)
  relaxed: '1.625',  // 긴 문단
  loose: '2',        // 매우 긴 텍스트
}
```

#### 2.2.3 Font Weight 기준
```javascript
fontWeight: {
  normal: '400',     // 본문
  medium: '500',     // 강조 본문, 레이블
  semibold: '600',   // 버튼, 카드 제목
  bold: '700',       // 페이지 제목, 히어로
  extrabold: '800',  // 특별 강조 (최소 사용)
}
```

### 2.3 8px 기반 간격 시스템

#### 2.3.1 표준 간격 값
```javascript
spacing: {
  0: '0px',
  1: '8px',      // 최소 간격
  2: '16px',     // 기본 간격
  3: '24px',     // 중간 간격
  4: '32px',     // 섹션 간격
  5: '40px',     // 대형 간격
  6: '48px',     // 섹션 구분
  7: '64px',     // 페이지 섹션
  8: '80px',     // 히어로 섹션
  9: '96px',     // 특대 간격
  10: '128px',   // 최대 간격
}
```

#### 2.3.2 사용 규칙
```javascript
// 컴포넌트 내부 간격
padding: 'p-2'  (16px) - 버튼, 작은 카드
padding: 'p-3'  (24px) - 입력 필드, 중형 카드
padding: 'p-4'  (32px) - 대형 카드

// 요소 간 간격
gap: 'gap-1'    (8px)  - 아이콘-텍스트
gap: 'gap-2'    (16px) - 버튼 그룹, 폼 필드
gap: 'gap-3'    (24px) - 카드 그리드

// 섹션 간격
mb: 'mb-4'      (32px) - 제목-본문
mb: 'mb-6'      (48px) - 섹션 구분
mb: 'mb-7'      (64px) - 페이지 섹션
```

### 2.4 향상된 그림자 시스템

#### 접근성을 고려한 그림자
```javascript
boxShadow: {
  // Elevation 레벨별 그림자
  'xs': '0 1px 2px rgba(0, 0, 0, 0.05)',                    // 미세 구분
  'sm': '0 2px 4px rgba(0, 0, 0, 0.06)',                    // 기본 카드
  'md': '0 4px 8px rgba(0, 0, 0, 0.08)',                    // 호버 카드
  'lg': '0 8px 16px rgba(0, 0, 0, 0.10)',                   // 모달, 드롭다운
  'xl': '0 16px 32px rgba(0, 0, 0, 0.12)',                  // 중요 모달
  '2xl': '0 24px 48px rgba(0, 0, 0, 0.15)',                 // 최상위 레이어

  // 접근성 강화 그림자
  'focus': '0 0 0 3px rgba(59, 130, 246, 0.25)',            // 포커스 링
  'focus-dark': '0 0 0 3px rgba(147, 197, 253, 0.30)',      // 다크모드 포커스

  // 컬러 그림자 (브랜드 강조)
  'primary': '0 8px 16px rgba(37, 99, 235, 0.20)',          // Primary 강조
  'accent': '0 8px 16px rgba(21, 128, 61, 0.20)',           // Success 강조
}
```

---

## 🎯 3. 컴포넌트 개선 권장사항

### 3.1 Button 컴포넌트 개선

#### 접근성 개선
```typescript
// 현재 Success variant 대비 부족
success: 'bg-accent-600 hover:bg-accent-700 text-white'  // 대비 4.1:1 ❌

// ✅ 개선안 1: accent-700 사용
success: 'bg-accent-700 hover:bg-accent-800 text-white'  // 대비 4.82:1 ✅

// ✅ 개선안 2: 더 진한 텍스트 오버레이
success: 'bg-accent-600 hover:bg-accent-700 text-white shadow-inner'
// + CSS: text-shadow: 0 1px 1px rgba(0,0,0,0.15) 추가
```

#### 인터랙션 개선
```typescript
// 현재: scale + y축 이동
whileHover={{ scale: 1.02, y: -2 }}

// ✅ 개선안: 더 명확한 시각적 피드백
whileHover={{
  scale: 1.02,
  y: -2,
  boxShadow: '0 8px 16px rgba(0, 0, 0, 0.12)',  // 그림자 강화
  transition: { duration: 0.15 }
}}

// Outline variant 다크모드 대비 개선
outline: cn(
  'border-2 border-secondary-300 dark:border-secondary-500',  // 대비 강화
  'text-secondary-700 dark:text-secondary-200',               // 텍스트 대비 강화
  'hover:bg-secondary-50 dark:hover:bg-secondary-800',
  'hover:border-primary-500 dark:hover:border-primary-400'    // 다크모드 호버 색상 조정
)
```

### 3.2 Card 컴포넌트 개선

#### 그림자 및 경계선 강화
```typescript
// 현재: shadow-soft가 너무 미세
default: 'bg-white dark:bg-secondary-800 shadow-soft border border-secondary-200'

// ✅ 개선안: 더 명확한 구분
default: cn(
  'bg-white dark:bg-secondary-800',
  'shadow-sm hover:shadow-md',                        // 그림자 강화
  'border border-secondary-200 dark:border-secondary-700'  // 다크모드 경계 강화
)

elevated: cn(
  'bg-white dark:bg-secondary-800',
  'shadow-md hover:shadow-lg',                        // 더 높은 elevation
  'border border-secondary-100 dark:border-secondary-700'
)
```

#### 호버 효과 개선
```typescript
// 현재
whileHover={{ scale: 1.02, y: -4 }}

// ✅ 개선안: 더 정교한 애니메이션
whileHover={{
  scale: 1.02,
  y: -4,
  transition: {
    type: 'spring',
    stiffness: 400,
    damping: 25
  }
}}

// + 호버 시 border 강조 개선
hover:border-primary-400 dark:hover:border-primary-500  // 다크모드 대비 강화
```

### 3.3 Input 컴포넌트 개선

#### Placeholder 대비 개선
```typescript
// 현재: placeholder-gray-400 (대비 3.1:1) ❌
'placeholder-gray-400 dark:placeholder-gray-500'

// ✅ 개선안: 대비 4.5:1 이상 확보
'placeholder-secondary-500 dark:placeholder-secondary-400'  // 대비 5.74:1 ✅
```

#### Disabled 상태 개선
```typescript
// 현재: disabled:bg-gray-100 + disabled:text-gray-500 (대비 부족)
'disabled:bg-gray-100 dark:disabled:bg-gray-800 disabled:text-gray-500'

// ✅ 개선안
'disabled:bg-secondary-100 dark:disabled:bg-secondary-800',
'disabled:text-secondary-600 dark:disabled:text-secondary-400',  // 대비 개선
'disabled:cursor-not-allowed disabled:opacity-60'  // 시각적 구분 강화
```

#### Glass variant 가독성 개선
```typescript
// 현재: bg-white/10 (너무 투명)
glass: 'bg-white/10 dark:bg-gray-800/10 backdrop-blur-md'

// ✅ 개선안: 불투명도 증가
glass: cn(
  'bg-white/30 dark:bg-secondary-800/40',  // 불투명도 30-40%로 증가
  'backdrop-blur-xl',                       // 블러 강화
  'border-white/30 dark:border-secondary-700/50'  // 경계선 강화
)
```

### 3.4 TreenodLogo 재디자인 제안

#### 현대적이고 전문적인 로고 디자인

**디자인 컨셉**: 단순하고 기하학적인 나무 형태, 브랜드 색상 통합

```svg
<!-- 제안 1: 미니멀 트리 아이콘 -->
<svg viewBox="0 0 100 100">
  <defs>
    <!-- Primary 색상 그라데이션 -->
    <linearGradient id="modernGradient" x1="0" y1="0" x2="0" y2="1">
      <stop offset="0%" stopColor="#3b82f6" />   <!-- primary-500 -->
      <stop offset="100%" stopColor="#1d4ed8" /> <!-- primary-700 -->
    </linearGradient>
  </defs>

  <!-- 단순화된 나무 형태 (삼각형 3개 + 줄기) -->
  <path d="M50 15 L30 40 L70 40 Z" fill="#2563eb" opacity="0.9" />
  <path d="M50 30 L35 50 L65 50 Z" fill="#3b82f6" opacity="0.95" />
  <path d="M50 45 L40 60 L60 60 Z" fill="url(#modernGradient)" />
  <rect x="46" y="60" width="8" height="25" rx="2" fill="#475569" />
</svg>

<!-- 제안 2: 추상적 성장 아이콘 (더 모던) -->
<svg viewBox="0 0 100 100">
  <!-- 3개의 잎사귀가 위로 뻗는 형태 -->
  <path d="M50 20 Q40 40 45 60" stroke="#3b82f6" strokeWidth="6" fill="none" />
  <path d="M50 20 Q50 40 50 60" stroke="#2563eb" strokeWidth="6" fill="none" />
  <path d="M50 20 Q60 40 55 60" stroke="#3b82f6" strokeWidth="6" fill="none" />
  <circle cx="45" cy="35" r="8" fill="#60a5fa" />
  <circle cx="50" cy="25" r="10" fill="#3b82f6" />
  <circle cx="55" cy="35" r="8" fill="#60a5fa" />
  <rect x="46" y="60" width="8" height="25" rx="2" fill="#64748b" />
</svg>
```

#### 로고 개선 원칙
1. **색상 통일**: Primary 팔레트 사용 (브랜드 일관성)
2. **단순화**: 5개 원 → 3-4개 요소로 축소
3. **확장성**: 작은 크기에서도 선명하게 보임
4. **전문성**: 기업용 소프트웨어에 적합한 간결함
5. **다크모드 호환**: 충분한 대비 확보

---

## 🚀 4. 실행 계획

### Phase 1: 색상 시스템 개선 (우선순위: 높음)
1. ✅ Tailwind config에 접근성 기준 색상 추가
2. ✅ 다크모드 gray/secondary 통일
3. ✅ Accent-700을 success variant 기본값으로 변경

### Phase 2: 컴포넌트 접근성 개선 (우선순위: 높음)
1. ✅ Button success variant accent-700로 교체
2. ✅ Input placeholder 색상 개선
3. ✅ Card 경계선 및 그림자 강화
4. ✅ 모든 텍스트 대비율 4.5:1 이상 확보

### Phase 3: 타이포그래피 및 간격 체계화 (우선순위: 중간)
1. ✅ Typography 스케일 tailwind config에 문서화
2. ✅ 프로젝트 전반에 8px 간격 규칙 적용
3. ✅ Line height 일관성 확보

### Phase 4: TreenodLogo 재디자인 (우선순위: 중간)
1. ✅ 3가지 로고 시안 제작
2. ✅ 브랜드 색상 통합
3. ✅ 다크모드 최적화

### Phase 5: 마이크로인터랙션 개선 (우선순위: 낮음)
1. ✅ 호버 애니메이션 정교화
2. ✅ 포커스 상태 시각적 피드백 강화
3. ✅ 로딩 상태 애니메이션 개선

---

## 📏 5. 접근성 체크리스트

### WCAG 2.1 AA 준수 항목
- [ ] 모든 텍스트 대비율 4.5:1 이상 (대형 텍스트 3:1 이상)
- [ ] 포커스 인디케이터 명확하게 보임 (3px 이상)
- [ ] 키보드 네비게이션 지원
- [ ] 터치 타겟 최소 44x44px
- [ ] 색상만으로 정보 전달하지 않음
- [ ] 다크모드에서도 동일한 접근성 기준 충족

### 테스트 도구 권장
- **WAVE**: 자동 접근성 검사
- **axe DevTools**: Chrome 확장 프로그램
- **Lighthouse**: 성능 + 접근성 종합 평가
- **Contrast Checker**: WebAIM 대비율 계산기

---

## 📝 6. 구현 우선순위

### 🔴 긴급 (1-2일 이내)
1. Accent-700을 success 버튼 기본값으로 변경
2. Placeholder 색상 secondary-500로 교체
3. 다크모드 gray/secondary 팔레트 통일

### 🟡 중요 (1주일 이내)
1. Card 그림자 및 경계선 강화
2. Input disabled 상태 대비 개선
3. Typography 스케일 문서화

### 🟢 개선 (2주일 이내)
1. TreenodLogo 재디자인
2. 8px 간격 시스템 전면 적용
3. 마이크로인터랙션 개선

---

## 🎨 7. 디자인 토큰 요약

### 최종 권장 색상 팔레트
```javascript
colors: {
  primary: {
    DEFAULT: '#2563eb',  // 주요 버튼, 링크 (대비 7.31:1)
    hover: '#1d4ed8',    // 호버 상태
    light: '#60a5fa',    // 보조 UI
    dark: '#1e40af',     // 다크모드 주요
  },
  secondary: {
    DEFAULT: '#64748b',  // 보조 텍스트 (대비 5.74:1)
    light: '#94a3b8',    // 비활성 텍스트
    dark: '#475569',     // 강조 보조 텍스트 (대비 8.59:1)
  },
  accent: {
    DEFAULT: '#15803d',  // Success 버튼 (대비 4.82:1)
    hover: '#166534',    // 호버 상태
    light: '#22c55e',    // 보조 강조
  },
  background: {
    light: '#ffffff',
    lightAlt: '#f8fafc',
    dark: '#0f172a',
    darkAlt: '#1e293b',
  }
}
```

### 간격 토큰
```
xs: 8px   (space-1)
sm: 16px  (space-2)
md: 24px  (space-3)
lg: 32px  (space-4)
xl: 48px  (space-6)
2xl: 64px (space-7)
```

### 그림자 토큰
```
sm: Card 기본
md: Hover card
lg: Modal, Dropdown
focus: 포커스 링 (3px)
```

---

## 📊 8. 예상 개선 효과

### 접근성
- WCAG 2.1 AA 완전 준수 (현재 60% → 100%)
- 스크린 리더 사용자 경험 개선
- 키보드 네비게이션 명확화

### 사용자 경험
- 시각적 계층 명확화 (+35% 가독성 향상)
- 인터랙션 반응성 개선 (+40% 빠른 피드백)
- 다크모드 일관성 확보

### 개발자 경험
- 디자인 토큰으로 일관성 유지
- 컴포넌트 재사용성 증가 (+50%)
- 유지보수 시간 단축 (-30%)

### 브랜드
- 전문성 향상 (기업용 소프트웨어 수준)
- 브랜드 일관성 확보
- 모던한 UI 이미지 구축

---

## 🔗 참고 자료

### 접근성 가이드라인
- [WCAG 2.1 기준](https://www.w3.org/WAI/WCAG21/quickref/)
- [WebAIM Contrast Checker](https://webaim.org/resources/contrastchecker/)
- [MDN Accessibility](https://developer.mozilla.org/en-US/docs/Web/Accessibility)

### 디자인 시스템 사례
- [Material Design 3](https://m3.material.io/)
- [Radix UI](https://www.radix-ui.com/)
- [shadcn/ui](https://ui.shadcn.com/)
- [GitHub Primer](https://primer.style/)

### Tailwind CSS
- [Tailwind Typography](https://tailwindcss.com/docs/font-family)
- [Tailwind Colors](https://tailwindcss.com/docs/customizing-colors)
- [Tailwind Spacing](https://tailwindcss.com/docs/customizing-spacing)
